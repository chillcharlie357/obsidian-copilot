{
  "name": "dsh-acp-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ACP server adapter bridging the Agent Client Protocol to a DeepSeek Harness (dsh web) instance",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@dsh-obsidian/acp-core": "workspace:*",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "@types/ws": "^8.5.12",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
